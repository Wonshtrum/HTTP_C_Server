/**
 * \file config.h
 * \brief config file of the server
 * \author Démolis Eloi
 */

/**
 * \def DEFAULT_PORT
 * \brief the default port used by the server
 */
#define DEFAULT_PORT 8001
/**
 * \def ROOT_DIR
 * \brief the root directory used by the server
 */
#define ROOT_DIR "html/"
/**
 * \def DEFAULT_PAGE
 * \brief the default file served by the server
 */
#define DEFAULT_PAGE "index.html"
